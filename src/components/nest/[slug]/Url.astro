---
import SectionLayout from '@components/common/SectionLayout.astro'
import type { Data } from './datas'

interface Props {
  data: Data
}

const { data } = Astro.props
---

<SectionLayout title="URL">
  <a href={data.url} target="_blank" rel="noopener noreferrer">{data.url}</a>
</SectionLayout>
